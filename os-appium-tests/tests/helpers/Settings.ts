import {DEFAULT_TIMEOUT, DEFAULT_TIMEOUT_INTERVAL} from "../constants"

const SELECTORS = {
    ANDROID: {
       SETTINGS_APP: '',
    },
    IOS: {
        SETTINGS_APP: 'com.apple.Preferences',
    },
};
